services:
  vaultwarden:
    image: vaultwarden/server:latest
    restart: unless-stopped
    networks:
      - public
    environment:
      - SERVICE_URL_VAULTWARDEN_80
      - DOMAIN=${SERVICE_URL_VAULTWARDEN_80}
    volumes:
      - data:/data/
    expose:
      - 80
      

volumes:
  data:
    name: vaultwarden
    external: false


networks:
  public:
    name: public
    external: true